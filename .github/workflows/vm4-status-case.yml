name: VM4 Status Case Probe
on:
  pull_request:
    types: [opened, synchronize, reopened]
permissions:
  contents: read
jobs:
  case-check:
    name: CI
    if: github.event.pull_request.title == 'VM4_STATUS_CASE_20260912'
    runs-on: ubuntu-latest
    steps:
      - run: echo 'exact uppercase CI check only'
