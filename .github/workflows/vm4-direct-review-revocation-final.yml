name: VM4 Direct Review Revocation PR14
on:
  pull_request:
    types: [opened, synchronize, reopened]
permissions:
  contents: read
jobs:
  review-revocation-pr14-hold:
    name: review-revocation-pr14-hold
    if: github.event.pull_request.title == 'VM4_WA_REVIEW_REVOCATION_PR14_20260912'
    runs-on: ubuntu-latest
    steps:
      - name: Hold direct merge finalization window
        run: sleep 120
      - name: Complete trusted check
        run: echo 'review-revocation-pr14-hold complete'
